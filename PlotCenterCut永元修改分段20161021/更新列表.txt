2016/9/22 星期六 17:02 
1.绘制图层调整，将切割图形最后绘制，以保证切割图形在最上一层；（解决用户反馈切割命令识别错误）；
2.用“有切割命令打开文件”分段；
3.切绘中心在与主板通讯过程中主板电源关闭、连接线断开时，切绘中心主动断开连接更新状态。
2016/10/25 星期二 17:08 
1.修改“有切割命令打开文件”分段错误问题（文件未按规定的长度分隔，分割成许多小文件）；
2.解决图形尺寸识别错误（plt文件中存在IP、SC命令,将文件中的坐标数据按命令进行缩放、平移）；
2016/11/2 星期三 14:10 
1.文件倾斜角度（DI命令修改）
2016/11/7 星期一 18:34 
1.文件分页后文字大小发生变化 修改
2016/11/9 星期三 16:34 
1.修改文件分离打印和切割数据时错误（当数据中的有重复点（前后两个PD xy坐标一样），分离数据时会多出一条线）。